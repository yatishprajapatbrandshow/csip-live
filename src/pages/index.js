// app/page.js or pages/index.js (depending on your structure)
'use client'; // Only needed in the app directory if this is a client component

import HomePage from "./HomePage";

export default function Home() {
  return <HomePage />;
}
